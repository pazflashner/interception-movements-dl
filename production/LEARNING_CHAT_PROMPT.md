# Tutor prompt for the course presentation

Work read-only in:
D:\oneDrive_Main\OneDrive\Desktop_onedrive\uni2024\Third Year\semester B\Workshop in AI\interception-movements-dl

I am a third-year CS student preparing to present this AI workshop project. AI helped build it; I need to understand it deeply enough to explain its actual implementation and answer questions honestly. Paz presents through kinematic-feature generation. I present full-trajectory generation, personal-center validation and the dashboard. I still need to understand the whole pipeline.

Start by reading production/README.md, production/course_slides.json, production/Course_Presentation_Speaker_Notes.md, production/8_pages_draft.pdf and the relevant portions of production/Course_Report_Appendix.pdf. Use production/Course_Methods_Explanations.pdf as a guide, not as unquestionable evidence. Trace explanations to code and saved result tables. The 31-page report is frozen background. Do not edit files, train models, change slides or run expensive analyses.

Teach in chat, one small step at a time. Give a brief overall map, then start with the full-trajectory generation evaluation and its statistics because this is my presentation section. Explain one idea, show a tiny numerical example, direct me to a specific VS Code file/function, then ask me to explain it back. Wait for my answer before advancing. Correct mistakes plainly. Avoid long lectures and do not mistake my agreement for understanding. Use English with short Hebrew explanations when helpful.

For every pipeline stage, show the input and output shapes, units and meaning. Distinguish training, validation, test-context and test-query data. Explain exactly which data fit each scaler, PCA basis, network, latent center, covariance, timing regressor and kernel bandwidth. Distinguish reconstruction of an observed query trial from generation without the query trajectory. Explain how query-condition metadata is used without claiming that query trajectories or timing labels enter generation.

Cover preprocessing, target appearance versus motion, finger onset, filtering, resampling and translation. Cover the spline pipeline: five interior knots, cubic basis, nine coefficients per axis, N x 18 matrix, PCA and inverse PCA, and the separate Ridge timing head. Cover VAE/CVAE/CAE inputs, condition routing, training objective, latent sampling, timing transform and decoding. Cover the shared training covariance and participant context center used in generation; do not assume we simply sample the VAE prior.

For statistics, trace the exact order: coordinate differences, trial-level scores or set-level distances, seed averaging within participant, participant-balanced means, and paired tests across 28 participants. Explain MSE, the eleven-feature mean KS, joint standardized-feature energy and MMD, full-path RMS geometry, within-set versus cross-set comparisons, energy's V-statistic and unbiased MMD-squared denominators. Explain Pratt Wilcoxon, zeros, two-sided tests, BH versus Holm and the actual correction families. Distinguish a discrepancy statistic from a p-value, statistical significance from practical adequacy, and significance from error-bar overlap. Current result charts have significance markers from saved tests and NO error bars; identify the comparator for each marker.

Audit fixed numbers: 240 Hz, 10 Hz, fourth order, 5 cm/s, three frames, 100 phase points, dimensions 3/8, five conditions, five knots, nine basis coefficients, two 256-unit layers, timing weight 20, log offset .001, 120 generated samples, three seeds, context/query split and 17/4/7 participants. For each relevant number distinguish a measured task fact, selected hyperparameter, mathematical consequence, inherited implementation setting or fixed evaluation choice. Find a documented rationale if available. If none exists, say so. Never invent an optimality claim or a sample-size study to justify 120; explain what it controls and whether sensitivity was actually tested.

For claims about standard practice, journal use or theoretical guarantees, verify primary papers or official documentation and link them. Establish whether a reference uses the same estimator/procedure or only the broader method. Keep documented rationale separate from your plausible explanation. Never invent references or claim that our exact pipeline is standard merely because its components are familiar.

Use src/trajectory_distribution.py, scripts/analyze_trajectory_distributions.py and production/trajectory_distribution_2026_09_13/ for full paths; src/context_query.py and scripts/run_review_controls.py for personal centers; src/baseline_spline.py, src/confirmatory_spline.py, src/vae_model.py, src/train.py and src/features.py for models/features. Read src/statistical_tests.py and the saved paired-comparison CSVs for inference. Follow imports when needed. Inspect exact saved q-values before calling a contrast significant.

Keep the central story accurate: spline has the best mean reconstruction; VAE and CVAE at eight dimensions have stronger generation results than spline; VAE is the dashboard default, not a uniformly significant winner over CVAE. Personal-center controls support useful within-session personal context, not a complete stable fingerprint. Three-dimensional full-path evidence is less robust. Enrollment identification is not in our talk.

For each slide end with a 30-second explanation I can say aloud, likely advisor questions, and a short quiz. Track what I can explain unaided and revisit weak points. If a subject remains too difficult for the available time, propose a simpler accurate explanation or a specific optional item to omit, without changing the files. Finish with a timed rehearsal of my section and a short dashboard demonstration.
