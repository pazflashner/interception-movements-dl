# Data
data/raw/
data/processed/
data/corrected_v*/
data/stimuli/
data/*.zip

# Models
models/*.pt
models/*.json

# Results: everything here is regenerable from the code plus the raw data.
# Run directories (results/runs/<name>/config.yaml + checkpoint.pt + history.json)
# stay local; the config.yaml in each one is the local record of that run.
results/
output/pdf/figures/
output/pdf/rendered/
output/release/Interception_Movements_Advisor_Package/
output/release/advisor_package_*/
.tmp/
tmp/
external/jason-submovements/

# Local snapshot of the superseded movement-onset study. The snapshot remains
# on disk for context and is recoverable from Git commit b2cfcd6, but current
# code and reports must never consume it.
archive/movement_only_2026-08-10/*
!archive/movement_only_2026-08-10/README.md
archive/runtime_cache_2026-09-02/
archive/pre_review_corrections_2026-09-02/

# Current study artifacts are reproducible and can be large. Protocols,
# summaries, and source code remain tracked; caches/checkpoints/exports do not.
studies/*/data/*.pkl
studies/*/data/**/*.pkl
studies/*/data/*.csv
studies/*/data/**/*.csv
studies/*/data/*.json
studies/*/data/**/*.json
studies/*/results/
studies/*/runs/
studies/*/output/
studies/*/data_audit/*.csv

# Office lock files (created while a .docx/.xlsx is open)
~$*

# Session artefacts
session-transcript.pdf

# Private correspondence and local audit working files; handoff is PAZ_REVIEW_HANDOFF.md.
jason resources/
review/
review_second_pass/
review_fresh_2026_09_05/

# Python
__pycache__/
.pytest_cache/
pytest-cache-files-*/
*.pyc
*.pyo
.eggs/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Production keeps manuscript builders, PDFs and compact evidence in Git.
# Large audit intermediates and rendered QA images remain reproducible locally.
production/assets/
production/verification_*/
production/*.log
production/audit_*/*.log
production/audit_*/*_log.txt
production/audit_*/*.pkl
production/audit_*/generation_audit/
production/audit_*/condition_shuffle_raw.csv
production/audit_*/ridge_precision_generated_threads*.csv
production/archive/
archive/review_cleanup_2026-09-08/review/
archive/review_cleanup_2026-09-08/review_second_pass/
archive/review_cleanup_2026-09-08/review_fresh_2026_09_05/
archive/review_cleanup_2026-09-08/runtime_scratch/
archive/review_cleanup_2026-09-08/correction_inputs/

# Figures extracted from the laboratory draft by production/build_submission_report.py.
# Derived from a tracked PDF and regenerated automatically on first run.
production/figures_extracted/
# Editable .docx drafts converted from delivered PDFs; the PDF stays authoritative.
output/docx/
