# Current review evidence (5 September 2026)

These compact tables support the eight-page report and appendix. Full participant,
donor and trial-level CSVs are in the committed advisor ZIP; restore them with
`python scripts/prepare_review_workspace.py`. These tables are outputs, not raw
recordings. See `../PAZ_REVIEW_HANDOFF.md` for interpretation and test commands.

`protocol.json` records the control settings; `checkpoints.json` records frozen
model hashes. `environment.json` records executed library versions. The main
paired table preserves SciPy settings. No raw emails or Dropbox recordings are
included. Final delivery checks are in `DELIVERY_CHECKS.json`.
