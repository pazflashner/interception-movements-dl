# Historical work

Archived work is preserved for context, not presented as current evidence.
Start with `../REVIEW_HANDOFF.md` and the final strategy study instead.

- `movement_only_2026-08-10/`: older movement-only snapshot; local artifacts are
  ignored by Git and its original README explains provenance.
- `pre_confirmatory_2026-09-02/`: superseded standalone entry points and study
  summary, moved unchanged during review preparation.
- `runtime_cache_2026-09-02/`: disposable local pytest cache folders, if movable;
  Git-ignored and unrelated to scientific evidence.

The live `studies/strategy_window_comparison/` directory was deliberately NOT
moved: canonical data, audit, real submovement fits, and a K-means result there
are still inputs to the final study.
